# WEB

```
VSCODE에서 ALT+B -> 브라우저 열림

<br>  -> 줄바꿈

class 선택자 : 주어진 클래스 속성을 가진 모든 요소 선택
.index -> class="index"가진 요서 선택

id 선택자 : class랑 똑같이 동작하지만 주어진 아이디 가진 요소가 하나만 있어야함

! 치면 html 기본 틀 완성

두개 동시 선택가능 ex) h1,h2 { color:red;}

자식 결합자 ex) class=green이고 span태그안에있다면
.green > span { font-size : 30px;}

자손 결합자(본인보다 하위 전체 선택) ex) .green li { color: brown;}
green과 li사이 띄어쓰기 (green 클래스 아래 있는 모든 li선택)

우선 순위
!important(이거 쓰면 무조건 우선순위 1등 근데 웬만하면 안씀)->인라인 스타일(이것도 안씀)->id 선택자->class 선택자 -> 요소 선택자

상속
부모 요소의 속성을 자식 요소에게 상속함
상속되는 속성도 있고 되지 않는 속성도 있음

추가사항
html 요소 이름 소문자로 쓰기
속성 따옴표는 큰따옴표 쓰기
html은 에러가 안나므로 작성 유의
웬만하면 속성은 class만 사용

웹자료 검색할 때 mdn쓰기
```
